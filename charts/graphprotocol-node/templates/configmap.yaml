apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "graphprotocol-node.fullname" . }}
  labels:
    {{- include "graphprotocol-node.labels" . | nindent 4 }}
data:
  BLOCK_INGESTOR: {{ include "graphprotocol-node.fullname" . }}-index-0 #TODO: Do we need this value to be configurable?
  GRAPH_KILL_IF_UNRESPONSIVE: "true" #TODO: Do we need this value to be configurable?
  postgres_db: {{ .Values.config.posgresDb }} #TODO: Add required check
  ipfs: {{ .Values.config.ipfs }} #TODO: Add required check
  ethereum: {{ .Values.config.ethereum }} #TODO: Add required check + check if it works well with multiline
