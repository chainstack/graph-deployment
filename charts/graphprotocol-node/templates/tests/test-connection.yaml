apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "graphprotocol-node.fullname" . }}-test-connection"
  labels:
    {{- include "graphprotocol-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "graphprotocol-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
